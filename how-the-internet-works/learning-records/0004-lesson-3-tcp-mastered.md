# Lesson 0003 (TCP) mastered — 5/5

Perfect quiz score including "why three packets not two" — the question flagged as where candidates freeze. Understands ISN negotiation, reordering via sequence numbers, FIN teardown, and when UDP beats TCP.

**Implications:** Transport layer is solid; lesson 0004 (TLS) can assume "TCP first, then TLS on top". Next zooms after TLS: HTTP request/response semantics.
