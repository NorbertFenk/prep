# Lesson 0004 (TLS) mastered — 5/5

Perfect quiz score: certificate chain of trust, why symmetric takes over after the handshake, MITM defense, Diffie-Hellman (nothing secret crosses the wire), and TLS 1.3's 1-RTT improvement.

**Implications:** Transport + security layers are solid. Lesson 0005 (HTTP) can assume "TCP+TLS is the tunnel, HTTP rides inside". Next zoom after HTTP: what the browser does with the response (parsing/rendering).
