# Lesson 0006 (rendering) mastered — 5/5

Perfect quiz score: why CSS is render-blocking, parser-blocking scripts vs defer, render tree composition, what layout computes, and the critical rendering path in order.

**Implications:** The full URL→pixels journey is complete. Lesson 0007 (performance) can assume every step's cost. Next zoom after performance: the server side — reverse proxies and load balancers.
